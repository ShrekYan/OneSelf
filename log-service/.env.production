# 服务端口
PORT=8890

# 运行环境
NODE_ENV=production

# 日志模块配置
# 服务标识（用于 Graylog 和日志区分）
LOGGER_HOSTNAME=log-service-prd

# Graylog 上游配置（默认禁用，需要时启用）
GRAYLOG_ENABLED=false
# GRAYLOG_HOST=graylog.example.com
# GRAYLOG_PORT=12201
