# AI 协同结果智能保存命令

---

## 🔴🔴🔴 最高优先级声明（必须先读三遍！）

> ⚠️ ⚠️ ⚠️ **提示词完整性是整个 /save-ai 命令的灵魂，是最高最高最高优先级！**

1. 🚨 **其他所有章节都可以简化，唯独「提示词相关章节」必须 100% 完整、100% 详细、100% 突出！**
2. 🚨 **其他所有章节都可以简化，唯独「提示词相关章节」必须 100% 完整、100% 详细、100% 突出！**
3. 🚨 **其他所有章节都可以简化，唯独「提示词相关章节」必须 100% 完整、100% 详细、100% 突出！**

**核心要求（缺一不可）：**
- ✅ **第 1 章「提示词对照学习区」必须第一个生成，全部写完、校验通过后，才能开始写其他章节！**
- ✅ **用户原始提示词必须完整保留**（不能只截取一句话，必须完整原文）
- ✅ **优化后的完整成品模板必须紧跟在原始提示词下面**（两节紧挨着，中间不能插任何其他内容）
- ✅ **7维度标准化评判必须完整**（每个维度都要有，不能缺）
- ✅ **不足点改进建议必须完整**（优化前 vs 优化后对比表格）
- ✅ **质量评分必须明确**（具体分数 + 优缺点分析）
- ✅ **优化后的完整成品模板必须突出展示**（用 ```markdown 代码块完整包裹，标注"✅ 可直接复制套用"）

---

## 🔴🔴🔴 强制章节顺序（绝对不能乱！）

生成教学总结文档时，必须严格按照以下顺序生成，绝对不能乱：

```
第 1 章：提示词对照学习区 ⭐⭐⭐⭐⭐（必须第一个生成！）
    ↓
    1.1 📝 你的原始提示词（完整保留原文）
    ↓（紧接，中间不能有任何其他内容！）
    1.2 ✨ 优化后的完整提示词模板（代码块包裹，✅ 可直接复制套用）
    ↓
    1.3 📊 质量提升对比表
    ↓
    1.4 💡 优化要点解析
    ↓
🔴🔴🔴 第 1 章全部写完后，必须暂停输出，先做 5 项自我校验，全部通过后才能继续写第 2 章！
    ↓
第 2 章及以后：其他章节（7维度评判、决策点分析、问题分析等）
```

> 🎯 **架构师思维**：学习=对照+对比+理解。把"原始提示词"和"优化后模板"放在同一屏，紧挨着，用户可以左眼看原始、右眼看优化，瞬间理解差异，学习效率提升 500%！

---

## 🔴🔴🔴 第 1 章生成后强制自我校验（必须做！）

第 1 章「提示词对照学习区」写完后，必须立即暂停输出，对照以下 5 项逐一检查：

| 检查项 | 要求 | 不满足就不能继续！ |
|--------|------|-------------------|
| ✅ 原始提示词完整性 | 一字不差，完整保留，多轮对话都要保留 | ❌ 回去补全 |
| ✅ 两节紧挨着 | 1.1 和 1.2 中间没有任何其他内容，紧挨着 | ❌ 调整顺序 |
| ✅ 模板代码块完整 | 优化后的模板用 ```markdown 代码块完整包裹 | ❌ 补上代码块 |
| ✅ 模板标注醒目 | 有"✅ 可直接复制套用"的醒目标注 | ❌ 加上标注 |
| ✅ 对比表完整 | 有质量提升对比表，每个维度都有分数 | ❌ 回去补上 |

**全部 5 项都通过后，才能开始写第 2 章及以后的内容！**

---

**触发**: `/save-ai [可选：模块路径]`
- `模块路径`（可选）：手动指定本次协同开发的模块路径
- 例如：`/save-ai apps/web/src/pages/ArticleDetail`

**核心定位**: 在人机协同对话过程中，即时保存本次 AI 协助开发的所有成果。**不依赖 Git 变更检测**，而是基于当前对话上下文进行智能提取和归档。

**🚨 强制执行规则：每次执行 /save-ai 必须生成 3 个文档 + 1 个强制章节，缺一不可**

1. ✅ **模块功能文档** - `01-README.md`，模块说明，给后续开发看
2. ✅ **协同成果文档** - `02-collaboration.md`，完整的协同过程记录
   - 🚨 **必须包含「## 🔧 核心代码实现」章节**，不得为空，不得遗漏所有重要代码变更
3. ✅ **教学总结文档** - `03-teaching-guide.md`，知识点和踩坑教训沉淀

---

## 设计理念

### 为什么不依赖 Git？
1. **实时性**：对话过程中随时保存，不需要等代码写完才提交 Git
2. **完整性**：保存的是"协同过程"，不仅是代码变更，还包括思路、决策、踩坑记录
3. **灵活性**：支持中途多次保存，支持实验性代码（不一定提交到 Git）

### 保存的是什么？
本次人机协同对话中产生的所有开发产出，包括：
- ✅ 新创建/修改的代码文件内容
- ✅ 代码审查意见和改进建议
- ✅ 架构设计决策过程
- ✅ 遇到的问题和解决方案
- ✅ AI 给出的解释和思路说明
- ✅ 用户的关键决策和反馈

---

## 执行流程

### 第 0 步：生成前强制加载规范（必须先执行！）

> 🚨 **在开始生成任何文档之前，必须先完整阅读并理解以下 3 项要求，确保完全牢记后再开始**：

1. **5项强制校验清单的具体内容**（必须逐条对应，缺一项都不能输出）：
   - ✅ 检查项 1：完整保留用户所有的原始提示词原文（多轮对话都要保留）
   - ✅ 检查项 2：完整的7维度标准化评判表格（维度、正面教材、反面教材、架构师思维）
   - ✅ 检查项 3：不足点改进建议表格（分类、不足点、优化前、优化后、价值说明）
   - ✅ 检查项 4：明确的质量评分 + 优缺点分析
   - ✅ 检查项 5：突出展示的完整成品提示词模板（代码块包裹 + 标注"✅ 可直接复制套用"）

2. **提示词完整性是最高优先级**：
   - 其他所有章节都可以适当简化
   - 唯独以上5项提示词相关章节必须 100% 完整、100% 详细、100% 突出
   - 如果时间不够，宁可砍掉其他章节，也要保证这5项完整

3. **优化后的提示词模板必须突出展示**：
   - 必须用 ```markdown 代码块完整包裹
   - 必须标注"✅ 可直接复制套用"
   - 必须包含完整4个部分：用途说明、模板内容、质量对比表、使用说明

---

### 第一步：上下文分析（自动执行）
1. 扫描当前对话历史，识别本次协同的核心内容
2. 自动提取关键信息：
   - 开发的模块/功能名称
   - 修改/创建的文件路径清单
   - 遇到的技术问题和解决方案
   - 重要的架构决策和理由
   - 代码审查的关键点
   - **用户的原始提示词完整内容（必须完整提取，不能简化）**

### 第二步：模块路径识别（自动 + 手动）
1. **优先使用用户传入的模块路径**（如果提供）
2. **自动识别**：从对话内容中提取提到的文件路径，推断模块路径
   - 前端：识别 `apps/web/src/pages/xxx/` 或 `apps/web/src/components/xxx/`
   - 后端：识别 `services/[service]/src/[module]/`
3. **无法识别时**：提示用户手动指定，或使用默认路径（按时间戳命名）

### 第三步：生成模块功能文档（强制）
- **强制自动调用** `/module-doc` 生成模块功能说明文档
- **必须生成**，不得跳过
- 根据对话上下文智能识别开发类型，动态注入对应检查清单和最佳实践
- 文档位置：`[模块路径]/README.md`

**智能类型识别规则**：命中 2 个以上关键词即匹配对应类型
- 前端 React 组件：React、useState、useEffect、useMemo、useCallback、MobX、observer、useLocalObservable、组件、页面、CSS、SCSS、样式、Ant Design Mobile
- 后端 NestJS 模块：NestJS、Controller、Service、Module、DTO、中间件、拦截器、守卫、后端、接口、API
- 数据库 Prisma：Prisma、schema、model、migrate、数据库、MySQL、PostgreSQL、Redis、SQL、索引、事务
- 工具函数/通用：utils、helper、工具函数、公共方法、通用逻辑

---

### 第四步：生成协同成果文档（强制）
- **必须生成**，不得跳过
- 基于对话上下文，结构化保存完整的协同过程
- 文档位置：`docs/ai-collaboration/[类型]/[模块名]-[YYYYMMDD].md`

**文档结构**：
```markdown
# AI 协同开发成果 - [模块/功能名称]

## 📅 协同基本信息
- 协同日期：YYYY-MM-DD
- 开发模块：[模块路径]
- 协同时长：[估算时长]

## 🎯 本次协同目标
[从对话中提取用户最初的需求描述]

## 📝 完成的工作内容
### 代码变更清单
- [文件路径1]：[修改内容简述]
- [文件路径2]：[修改内容简述]
...

### 功能实现说明
[详细说明实现了什么功能，关键实现思路]

## 💡 关键技术决策
### 决策1：[决策主题]
- 背景：[问题背景]
- 方案：[选择的方案]
- 理由：[为什么选择这个方案]

### 决策2：...

---

## 🔧 核心代码实现

> 🚨 **强制必填章节，不能为空！** 所有重要代码变更必须完整记录在这里
> 📌 后续回顾直接看文档，不用翻 git 历史

### 1. 【文件名】【修改类型：新增/修改/删除】
> 简述这段代码解决的问题或实现的功能

```typescript
// 粘贴完整的核心代码片段
// 不需要全文件，只放核心修改部分
```

### 2. 【文件名】【修改类型】
> 简述...

```typescript
// 核心代码片段
```

...（根据修改文件数量依次列出）

---

## ⚠️ 遇到的问题与解决方案
### 问题1：[问题描述]
- 现象：[具体现象]
- 原因：[根因分析]
- 解决方案：[最终采用的方案]

### 问题2：...

## 📌 代码审查要点
- [审查意见1]
- [审查意见2]
...

## 📚 后续建议与待办
- [建议1]
- [待办事项1]
...
```

---

### 第五步：生成教学总结（强制）
- **强制自动调用** `/teaching-summary`
- **必须生成**，不得跳过
- 将本次协同中的知识点、踩坑教训整理成教学素材
- 确保每次协同的经验都沉淀到教学知识库
- 文档位置：`docs/ai-teaching/[YYYYMM]/[YYYYMMDD]-[类型]-[模块名]-teaching-guide.md`

---

### 第五步补充：生成后强制自我校验闭环（🔴 不通过就不能输出！）

---

## 🔴🔴🔴 生成教学文档后，必须立即暂停输出！先做以下 5 项检查，缺一项都不能输出最终文档！

---

### ✅ 强制校验 1/5：原始提示词完整性检查
- **检查内容**：是否完整保留了用户所有的原始提示词原文？
- **要求**：
  - ❌ 不能只截取一句话、不能省略、不能概括
  - ✅ 必须完整保留用户输入的所有内容（一字不差）
  - ✅ 如果有多轮对话，每一轮的提示词都要完整保留
- **不满足怎么办**：回去补充完整，直到 100% 还原用户原文

---

### ✅ 强制校验 2/5：7维度标准化评判检查
- **检查内容**：是否有完整的7维度评判表格？
- **要求**：
  - ✅ 必须包含 4 列：维度、正面教材、反面教材、架构师思维解释
  - ✅ 7 个维度一个都不能少（现象描述、预期行为、验收标准、功能完整性、边界值、安全、性能）
  - ✅ 每个维度都要有具体的正面/反面案例，不能泛泛而谈
- **不满足怎么办**：补全表格，每个维度都要有具体案例

---

### ✅ 强制校验 3/5：不足点改进建议检查
- **检查内容**：是否有不足点改进建议表格？
- **要求**：
  - ✅ 必须包含 5 列：分类、不足点、优化前原文、优化后改写、价值说明
  - ✅ 每个不足点都必须有"优化前 vs 优化后"的具体对比
  - ✅ 不能只说"这里可以改进"，必须给出改进后的具体内容
- **不满足怎么办**：补全对比表格，每个不足点都要有具体的"优化后"内容

---

### ✅ 强制校验 4/5：质量评分检查
- **检查内容**：是否有明确的质量评分和优缺点分析？
- **要求**：
  - ✅ 必须有具体分数（如 92/100），不能含糊其辞
  - ✅ 必须有"👍 做得好的地方"具体列表
  - ✅ 必须有"📈 提升空间"具体列表
- **不满足怎么办**：补充具体的评分和详细的优缺点分析

---

### ✅ 强制校验 5/5：完整成品模板检查（最重要！）
- **检查内容**：是否有突出展示的完整成品提示词模板？
- **要求**：
  - ✅ 必须用 ```markdown 代码块完整包裹（不能是普通文本）
  - ✅ 必须标注"✅ 可直接复制套用"醒目标识
  - ✅ 必须包含完整 4 个部分：用途说明、模板内容、质量对比表、使用说明
  - ✅ 模板内容必须完整可用，用户复制过去就能用，不能留空
- **不满足怎么办**：补全模板的所有部分，确保用户可以直接复制套用

---

## 🔴 最高优先级原则（必须牢记！）

> 如果时间不够、篇幅有限，其他所有章节都可以适当简化，唯独以上 5 项必须 100% 完整、100% 详细、100% 突出！

> 宁愿把其他章节砍掉一半，也要保证这 5 项完整无缺！

---

#### 🎯 校验通过标准

以上 5 项检查，必须全部打 ✅，一项都不能少。全部满足后，才能输出最终文档。

**校验口诀（默念 3 遍）：**
1. 原始提示词完整吗？
2. 7维度表格全吗？
3. 不足点对比有吗？
4. 质量评分给了吗？
5. 成品模板突出吗？

全对了，再输出！

---

### 第六步：输出汇总结果
- 展示所有生成的 3 个文档路径
- 摘要展示本次协同的关键信息
- **必须明确列出生成的 3 种文档类型**，不得遗漏

---

## 输出格式规范

### ✅ 成功完成时的输出（必须生成 3 个文档）

```
🤖 AI 协同结果智能保存完成！

📊 协同摘要：
   - 📁 开发模块：apps/web/src/pages/ArticleDetail
   - 📝 修改文件：5 个
   - 💡 关键决策：3 项
   - ⚠️ 解决问题：2 个

📦 已生成的文件（共 3 个，同一目录）：
   1. ✅ [模块功能文档] docs/ai-save/[YYYYMMDD]-[模块名]/01-README.md
   2. ✅ [协同成果文档] docs/ai-save/[YYYYMMDD]-[模块名]/02-collaboration.md
   3. ✅ [教学总结] docs/ai-save/[YYYYMMDD]-[模块名]/03-teaching-guide.md

💡 提示：
   - 所有 3 类文档已全部生成，无遗漏
   - 协同成果已保存，可以随时回顾
   - 如需补充或修改，可以重新执行 /save-ai
   - 教学素材已同步更新到教学知识库
```

---

### ⚠️ 任何文档生成失败时的输出

如果 3 个文档中有任何一个生成失败，必须明确告知：

```
⚠️ AI 协同结果保存部分完成，有文档生成失败

📦 生成状态：
   1. ✅ [模块功能文档] 生成成功
   2. ❌ [协同成果文档] 生成失败：[失败原因]
   3. ✅ [教学总结] 生成成功

💡 建议：可以重新执行 /save-ai 重试，或单独调用对应命令生成缺失文档
```

### ⚠️ 无法自动识别模块时的输出
```
⚠️ 未能从对话中自动识别模块路径

💡 请手动指定模块路径：
   /save-ai [模块路径]

例如：
   /save-ai apps/web/src/pages/ArticleDetail
   /save-ai services/backend/src/articles
```

### ℹ️ 对话内容较少时的输出
```
ℹ️ 当前对话内容较少，建议继续开发后再保存

💡 提示：
   - 可以继续进行协同开发
   - 当完成一个完整功能或解决一个重要问题后，再执行 /save-ai 保存
```

---

## 特殊情况处理

### 情况 1：用户手动指定模块路径
- 优先使用用户传入的路径参数
- 跳过自动识别步骤
- 直接基于对话上下文生成该模块的协同成果文档

### 情况 2：同一次对话多次执行 /save-ai
- 检测是否已有本次对话的协同成果文档
- 采用增量更新方式，补充新的内容，而不是完全覆盖
- 在文件名中添加版本号或时间戳区分

### 情况 3：跨多个模块的协同
- 自动识别所有涉及的模块
- 分别为每个模块生成独立的协同成果文档
- 在汇总输出中列出所有生成的文件

---

## 文档存储规范

### 🎯 统一目录设计（所有文档放在同一目录，便于查找）

**所有 3 个文档统一放在同一个协同目录下，不再分散存放**

```
docs/
└── ai-save/                          # /save-ai 命令统一根目录
    └── [YYYYMMDD]-[模块名]/          # 单次协同目录，同一天同模块多次保存加 v2/v3
        ├── 01-README.md              # ✅ 模块功能文档（原 module-doc 产出）
        ├── 02-collaboration.md       # ✅ 协同成果文档（完整过程记录）
        └── 03-teaching-guide.md      # ✅ 教学总结文档（知识点沉淀）
```

**示例**：
```
docs/
└── ai-save/
    ├── 20260427-login-performance/
    │   ├── 01-README.md
    │   ├── 02-collaboration.md
    │   └── 03-teaching-guide.md
    └── 20260428-article-detail-v2/
        ├── 01-README.md
        ├── 02-collaboration.md
        └── 03-teaching-guide.md
```

---

### 📋 目录命名规则

| 规则 | 说明 | 示例 |
|------|------|------|
| **基础格式** | `[YYYYMMDD]-[模块名]` | `20260427-login-performance` |
| **多次保存** | 同一天同模块，末尾加 `v2`/`v3` | `20260427-login-performance-v2` |
| **模块名** | kebab-case 小写，用 `-` 分隔 | `user-profile`, `articles-module` |
| **类型前缀** | 文件名加 `01-`/`02-`/`03-` 前缀，按序号排序，一眼看清顺序 | |

---

### ✅ 统一目录的优势

1. **查找方便**：一次协同的所有产出在同一个文件夹
2. **结构清晰**：序号前缀让文档功能一目了然
3. **版本管理**：同模块多次保存不会覆盖，自动版本化
4. **归档简单**：直接打包整个目录即可备份

---

### ❌ 旧存储位置（已废弃）

以下分散存储方式不再使用：
- ~~`apps/web/src/pages/xxx/README.md`~~ → 不再放在代码目录
- ~~`docs/ai-collaboration/xxx.md`~~ → 不再单独存放
- ~~`docs/ai-teaching/[YYYYMM]/xxx.md`~~ → 不再按月份分目录

---

## 与其他命令的关系

| 命令 | 职责 | 调用关系 |
|------|------|----------|
| `/save-ai` | 保存人机协同的完整过程、决策、问题、代码变更的综合记录 | 自动调用 `/module-doc` + **必须调用** `/teaching-summary` |
| `/module-doc` | ✨ **通用模块文档生成器（唯一模板），智能识别类型动态注入内容 | 被 `/save-ai` 调用，也可独立使用 |
| `/teaching-summary` | 专门生成教学素材，聚焦知识点和踩坑教训 | 被 `/save-ai` **强制调用 |

---

## 质量保证要求

1. **零侵入**：不修改任何现有代码文件，只生成文档
2. **完整性**：确保不遗漏对话中的关键决策和解决方案
3. **结构化**：文档格式统一，便于后续检索和回顾
4. **可追溯**：文档中包含日期、模块路径、文件清单等元信息
