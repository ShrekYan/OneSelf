# 示例交互

- "全量安全扫描。"
- "扫描 src/business/XXX 是否存在 XSS 风险。"
- "检查当前变更是否有敏感信息泄露。"
- "审计这个页面的关键业务参数是否可被篡改。"
- "检查 WebView JSBridge 调用是否安全。"
- "分析当前项目 localStorage/sessionStorage 是否存储敏感信息。"
- "检查 package.json 和 lock 文件是否有供应链风险。"
- "审计当前 H5 页面在微信内置浏览器下的安全风险。"
- "检查跨窗口消息是否校验来源。"
- "检查生产构建是否可能暴露 SourceMap 或内部环境信息。"