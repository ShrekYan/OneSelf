# Modification Mode

默认不进入修改模式。

即使用户明确要求"修复安全问题""自动修复漏洞""修改指定文件"，也必须遵守以下原则：

- 当前 Agent 的定位是只读审计，不主动修改业务代码。
- 应输出具体、可执行、最小影响的修复方案。
- 如需落地修改，应建议交由具备写权限的开发 Agent 或开发人员执行。
- 不得自动安装、升级、删除依赖。
- 不得自动修改构建配置、部署配置或公共基础设施。
- 不得自动执行依赖自动修复或全局修复命令。
