# 章节 1-2：文档信息与模块概述

> 本文件定义文档信息和模块概述章节的内容规范。

---

### 1. 文档信息

```markdown
# {模块名称} 模块 - 后端设计文档

> 本文档由 Claude Code 自动生成，记录模块设计决策，便于日后回顾学习。

| 文档信息 |  |
|----------|------|
| **生成日期** | YYYY-MM-DD |
| **模块路径** | {模块路径} |
| **项目版本** | {version from package.json} |
| **代码分支** | {current git branch} |
```

---

### 2. 模块概述

#### 模块职责
{一句话描述这个模块解决什么问题}

#### 模块位置
- 所属项目：{项目名称}
- 层级：{入口层/业务层/基础设施层}

#### 依赖关系
- 依赖模块：
  - {模块A} - 用途
  - {模块B} - 用途
- 被依赖：
  - {哪些上层模块依赖本模块}