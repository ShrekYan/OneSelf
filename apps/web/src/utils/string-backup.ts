export function backupTrim(str: string): string {
  return str.trim();
}

export function backupUppercase(str: string): string {
  return str.toUpperCase();
}

export function backupLowercase(str: string): string {
  return str.toLowerCase();
}
